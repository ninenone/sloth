import React, { FunctionComponent } from 'react'
import { View } from 'react-native'
import styles from './styles'


type Props = {}

const ${screenName}: FunctionComponent<Props> = (props: Props) => {
    return (
       <View style={styles.container}>
         <View />
       </View>
    )
}

export default ${screenName}